{
  "name": "bolt-starter-ts",
  "version": "1.0.0",
  "description": "Bolt app sample in TypeScript",
  "main": "lib/index.js",
  "scripts": {
    "start": "npm run build && node lib/main.js",
    "start:oauth": "npm run build && node lib/mainOAuthApp.js",
    "local": "npm run build:live",
    "local:oauth": "npm run build:live",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec \"ts-node\" src/main.ts",
    "build:live:oauth": "nodemon --watch 'src/**/*.ts' --exec \"ts-node\" src/mainOAuthApp.ts"
  },
  "author": "Kazuhiro Sera (@seratch)",
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.11.0",
    "@types/node": "^14.14.20",
    "dotenv": "^8.2.0",
    "jsonwebtoken": "^9.0.0",
    "nodemon": "^2.0.20",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
